Const astral = require('astral.js@6.0.4')
Const db = require('astral.ts')

Const config = require('config.json')

Token.new.start
Astral.event.load('tout les évents ont été lancer avec succès !')

astral.new.connect('connecter au ${client} qui possède ${serveur} serveurs !')

astral.commande{(
Nom: "ping",
Code: `la latence de votre bot est de As${ping} ms.`
)}

astral.config.token('login.success')